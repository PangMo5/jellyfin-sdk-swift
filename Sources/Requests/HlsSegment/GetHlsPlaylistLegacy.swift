//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension JellyfinAPI.HlsSegment {

    /** Gets a hls video playlist. */
    public enum GetHlsPlaylistLegacy {

        public static let service = APIService<Response>(id: "GetHlsPlaylistLegacy", tag: "HlsSegment", method: "GET", path: "/Videos/{itemId}/hls/{playlistId}/stream.m3u8", hasBody: false, securityRequirements: [SecurityRequirement(type: "CustomAuthentication", scopes: ["DefaultAuthorization"])])

        public final class Request: APIRequest<Response> {

            public struct Options {

                /** The video id. */
                public var itemId: String

                /** The playlist id. */
                public var playlistId: String

                public init(itemId: String, playlistId: String) {
                    self.itemId = itemId
                    self.playlistId = playlistId
                }
            }

            public var options: Options

            public init(options: Options) {
                self.options = options
                super.init(service: GetHlsPlaylistLegacy.service)
            }

            /// convenience initialiser so an Option doesn't have to be created
            public convenience init(itemId: String, playlistId: String) {
                let options = Options(itemId: itemId, playlistId: playlistId)
                self.init(options: options)
            }

            public override var path: String {
                return super.path.replacingOccurrences(of: "{" + "itemId" + "}", with: "\(self.options.itemId)").replacingOccurrences(of: "{" + "playlistId" + "}", with: "\(self.options.playlistId)")
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {
            public typealias SuccessType = File

            /** Hls video playlist returned. */
            case status200(File)

            /** Unauthorized */
            case status401

            /** Forbidden */
            case status403

            public var success: File? {
                switch self {
                case .status200(let response): return response
                default: return nil
                }
            }

            public var response: Any {
                switch self {
                case .status200(let response): return response
                default: return ()
                }
            }

            public var statusCode: Int {
                switch self {
                case .status200: return 200
                case .status401: return 401
                case .status403: return 403
                }
            }

            public var successful: Bool {
                switch self {
                case .status200: return true
                case .status401: return false
                case .status403: return false
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 200: self = try .status200(data)
                case 401: self = .status401
                case 403: self = .status403
                default: throw APIClientError.unexpectedStatusCode(statusCode: statusCode, data: data)
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
