//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension JellyfinAPI.Movies {

    /** Gets movie recommendations. */
    public enum GetMovieRecommendations {

        public static let service = APIService<Response>(id: "GetMovieRecommendations", tag: "Movies", method: "GET", path: "/Movies/Recommendations", hasBody: false, securityRequirements: [SecurityRequirement(type: "CustomAuthentication", scopes: ["DefaultAuthorization"])])

        public final class Request: APIRequest<Response> {

            public struct Options {

                /** Optional. Filter by user id, and attach user data. */
                public var userId: ID?

                /** Specify this to localize the search to a specific item or folder. Omit to use the root. */
                public var parentId: ID?

                /** Optional. The fields to return. */
                public var fields: [ItemFields]?

                /** The max number of categories to return. */
                public var categoryLimit: Int?

                /** The max number of items to return per category. */
                public var itemLimit: Int?

                public init(userId: ID? = nil, parentId: ID? = nil, fields: [ItemFields]? = nil, categoryLimit: Int? = nil, itemLimit: Int? = nil) {
                    self.userId = userId
                    self.parentId = parentId
                    self.fields = fields
                    self.categoryLimit = categoryLimit
                    self.itemLimit = itemLimit
                }
            }

            public var options: Options

            public init(options: Options) {
                self.options = options
                super.init(service: GetMovieRecommendations.service)
            }

            /// convenience initialiser so an Option doesn't have to be created
            public convenience init(userId: ID? = nil, parentId: ID? = nil, fields: [ItemFields]? = nil, categoryLimit: Int? = nil, itemLimit: Int? = nil) {
                let options = Options(userId: userId, parentId: parentId, fields: fields, categoryLimit: categoryLimit, itemLimit: itemLimit)
                self.init(options: options)
            }

            public override var queryParameters: [String: Any] {
                var params: [String: Any] = [:]
                if let userId = options.userId?.encode() {
                  params["userId"] = userId
                }
                if let parentId = options.parentId?.encode() {
                  params["parentId"] = parentId
                }
                if let fields = options.fields?.encode().map({ String(describing: $0) }).joined(separator: ",") {
                  params["fields"] = fields
                }
                if let categoryLimit = options.categoryLimit {
                  params["categoryLimit"] = categoryLimit
                }
                if let itemLimit = options.itemLimit {
                  params["itemLimit"] = itemLimit
                }
                return params
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {
            public typealias SuccessType = [RecommendationDtoModel]

            /** Movie recommendations returned. */
            case status200([RecommendationDtoModel])

            /** Unauthorized */
            case status401

            /** Forbidden */
            case status403

            public var success: [RecommendationDtoModel]? {
                switch self {
                case .status200(let response): return response
                default: return nil
                }
            }

            public var response: Any {
                switch self {
                case .status200(let response): return response
                default: return ()
                }
            }

            public var statusCode: Int {
                switch self {
                case .status200: return 200
                case .status401: return 401
                case .status403: return 403
                }
            }

            public var successful: Bool {
                switch self {
                case .status200: return true
                case .status401: return false
                case .status403: return false
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 200: self = try .status200(decoder.decode([RecommendationDtoModel].self, from: data))
                case 401: self = .status401
                case 403: self = .status403
                default: throw APIClientError.unexpectedStatusCode(statusCode: statusCode, data: data)
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
