//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** A list of notifications with the total record count for pagination. */
public struct NotificationResultDtoModel: APIModel {

    /** Gets or sets the current page of notifications. */
    public var notifications: [NotificationDtoModel]?

    /** Gets or sets the total number of notifications. */
    public var totalRecordCount: Int?

    public init(notifications: [NotificationDtoModel]? = nil, totalRecordCount: Int? = nil) {
        self.notifications = notifications
        self.totalRecordCount = totalRecordCount
    }

    public init(from decoder: Decoder) throws {
        let decoderContainer = try decoder.container(keyedBy: StringCodingKey.self)

        notifications = try decoderContainer.decodeArrayIfPresent("Notifications")
        totalRecordCount = try decoderContainer.decodeIfPresent("TotalRecordCount")
    }

    public func encode(to encoder: Encoder) throws {
        var encoderContainer = encoder.container(keyedBy: StringCodingKey.self)

        try encoderContainer.encodeIfPresent(notifications, forKey: "Notifications")
        try encoderContainer.encodeIfPresent(totalRecordCount, forKey: "TotalRecordCount")
    }

}
